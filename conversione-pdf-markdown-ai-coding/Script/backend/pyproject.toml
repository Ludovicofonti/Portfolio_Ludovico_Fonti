[project]
name = "pdf-to-markdown"
version = "0.1.0"
description = "Convert PDF documents to clean Markdown using local OCR"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
